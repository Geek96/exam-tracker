# P5–P8 功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 ROADMAP P5–P8 四组功能：DeepSeek API 接入、批量 PDF 转换 UX 优化、TOC 来源选择、UI 交互改进（资料删除二步确认 + AI 文件选择）

**Architecture:** 所有改动均在现有纯浏览器 + Vercel Serverless 架构内完成，无需新增第三方库。DeepSeek 使用 OpenAI 兼容 API。TOC 来源选择复用现有 modal substate 机制。AI 文件选择状态存入 localStorage。

**Tech Stack:** Vanilla JS / HTML / CSS，Vercel Serverless（Node.js CJS），localStorage，IndexedDB

> **版本规则：** 每次修改 `course.js` 后必须同步递增 `course.html` 中 `?v=N` 且更新 `AGENT_GUIDELINES.md` 中的版本记录。

---

## 文件变更清单

| 文件 | 任务 | 操作 |
|---|---|---|
| `api/study-plan.js` | P5 | 修改（添加 DeepSeek handler，共 ~35 行） |
| `course.html` | P5/P6/P7/P8 | 修改（model toggle + modal HTML + version bump） |
| `course.js` | P5/P6/P7/P8 | 修改（逻辑变更，每 task 递增版本号） |
| `course.css` | P7/P8 | 修改（新增 `.pdf-source-choice`、`.btn-mat-del-confirm`、`.btn-mat-sel` 样式） |
| `strings.js` | P5/P6/P7/P8 | 修改（新增 zh/en/es 字符串键） |
| `.agents/AGENT_GUIDELINES.md` | 每 task | 修改（更新当前版本号记录） |

---

## Task 1 — P5：DeepSeek API 后端接入

**Files:**
- Modify: `api/study-plan.js:37` (provider 分支)
- Modify: `api/study-plan.js:84` (插入 handleDeepSeek 函数)
- Modify: `course.html:157` (model toggle 按钮)
- Modify: `course.html:496` (version v22 → v23)
- Modify: `.agents/AGENT_GUIDELINES.md` (当前版本更新为 v=23)

### 背景

`api/study-plan.js` 已有 `handleClaude` 和 `handleGemini`。DeepSeek 使用 OpenAI 兼容 API（相同 SSE 格式）。前端 model toggle 已用 `data-provider` 驱动，只需加一个按钮。

- [ ] **Step 1: 在 `api/study-plan.js:37` 添加 deepseek 分支**

在 `if (provider === 'claude')` 的 else 前插入：

```js
  if (provider === 'claude') {
    await handleClaude(system, msgs, res);
  } else if (provider === 'deepseek') {
    await handleDeepSeek(system, msgs, res);
  } else {
    await handleGemini(system, msgs, res);
  }
```

（替换原来的 if/else 两分支）

- [ ] **Step 2: 在 `api/study-plan.js` 的 `handleClaude` 函数结束后（约 line 84）插入 `handleDeepSeek`**

```js
async function handleDeepSeek(system, messages, res) {
  var apiKey = process.env.DEEPSEEK_API_KEY;
  if (!apiKey) { res.status(500).json({ error: 'DEEPSEEK_API_KEY 未在 Vercel 环境变量中配置' }); return; }

  var contents = [{ role: 'system', content: system }].concat(
    messages.map(function(m) { return { role: m.role, content: m.content }; })
  );

  var upstream;
  try {
    upstream = await fetch('https://api.deepseek.com/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': 'Bearer ' + apiKey,
      },
      body: JSON.stringify({
        model: 'deepseek-chat',
        stream: true,
        max_tokens: 4096,
        messages: contents,
      }),
    });
  } catch (err) {
    res.status(502).json({ error: '无法连接 DeepSeek：' + err.message }); return;
  }

  if (!upstream.ok) {
    var raw = await upstream.text();
    var msg = raw;
    try { msg = JSON.parse(raw).error.message || raw; } catch {}
    res.status(upstream.status).json({ error: msg }); return;
  }

  await streamSSE(upstream.body, res, function(line) {
    try {
      var ev = JSON.parse(line);
      if (ev.choices && ev.choices[0] && ev.choices[0].delta && ev.choices[0].delta.content) {
        return ev.choices[0].delta.content;
      }
    } catch {}
  });
}
```

- [ ] **Step 3: 在 `course.html:156` 的 model toggle 区域添加 DeepSeek 按钮**

找到：
```html
              <button class="ai-model-btn" data-provider="claude">Claude</button>
```
在其后添加：
```html
              <button class="ai-model-btn" data-provider="deepseek">DeepSeek</button>
```

- [ ] **Step 4: 在 `course.html` 底部将 `course.js?v=22` 改为 `course.js?v=23`**

- [ ] **Step 5: 在 `.agents/AGENT_GUIDELINES.md` 将 "当前版本：`v=22`" 更新为 "当前版本：`v=23`"**

- [ ] **Step 6: 在 Vercel Dashboard 配置 `DEEPSEEK_API_KEY` 环境变量（Production + Preview）**

  提示用户：登录 Vercel → 项目设置 → Environment Variables → 添加 `DEEPSEEK_API_KEY`

- [ ] **Step 7: 手动验证**

  部署后打开 course 页面 → 点击 "DeepSeek" 按钮 → 发送消息 → 确认流式响应正常输出

- [ ] **Step 8: Commit**

```bash
git add api/study-plan.js course.html .agents/AGENT_GUIDELINES.md
git commit -m "feat: add DeepSeek API provider to AI study assistant (v=23)"
```

---

## Task 2 — P6：批量 PDF 转换 UX 优化

**Files:**
- Modify: `course.js:1538-1543` (uploadMaterialFiles PDF toast 逻辑)
- Modify: `strings.js:98` (添加 `batchConvertStart` 键)
- Modify: `course.html:496` (version v23 → v24)
- Modify: `.agents/AGENT_GUIDELINES.md` (更新为 v=24)

### 背景

`materialFileInput` 已有 `multiple` 属性，`uploadMaterialFiles` 已可处理多文件。问题在于每个 PDF 单独弹一个 toast，N 个 PDF 产生 N 条 toast，体验差。只需合并为一条汇总提示。

- [ ] **Step 1: 修改 `course.js` 的 `uploadMaterialFiles` 函数（约 line 1538）**

找到：
```js
  // PDFs: convert to Markdown via MinerU (background)
  for (const file of pdfs) {
    showToast(`📄 ${file.name} ${window.t('convertingToMd')}`);
    startMaterialMineruJob(file, 'pdf');
  }
```

替换为：
```js
  // PDFs: convert to Markdown via MinerU (background)
  if (pdfs.length === 1) {
    showToast(`📄 ${pdfs[0].name} ${window.t('convertingToMd')}`);
  } else if (pdfs.length > 1) {
    showToast(window.tf('batchConvertStart', { n: pdfs.length }));
  }
  for (const file of pdfs) {
    startMaterialMineruJob(file, 'pdf');
  }
```

- [ ] **Step 2: 在 `strings.js` 的三个语言块各添加 `batchConvertStart` 键**

在 zh 块（约 line 98，`convertingToMd` 后面）添加：
```js
    batchConvertStart: '📄 已开始转换 {n} 个 PDF 文件…',
```

在 en 块对应位置添加：
```js
    batchConvertStart: '📄 Converting {n} PDF files…',
```

在 es 块对应位置添加：
```js
    batchConvertStart: '📄 Convirtiendo {n} archivos PDF…',
```

- [ ] **Step 3: 将 `course.html` 底部版本号从 `v=23` 改为 `v=24`**

- [ ] **Step 4: 更新 `.agents/AGENT_GUIDELINES.md` 当前版本为 `v=24`**

- [ ] **Step 5: 手动验证**

  打开 course 页面 → 课程资料区 → 同时选择 3 个 PDF → 确认只出现 1 条 "已开始转换 3 个 PDF 文件…" toast，而不是 3 条

- [ ] **Step 6: Commit**

```bash
git add course.js strings.js course.html .agents/AGENT_GUIDELINES.md
git commit -m "feat: consolidate batch PDF conversion toast into single summary (v=24)"
```

---

## Task 3 — P7：章节目录来源选择

**Files:**
- Modify: `course.html:282` (在 `pdf-no-outline` div 前插入 `pdf-source-choice` div)
- Modify: `course.html:496` (version v24 → v25)
- Modify: `course.css` (添加 `.pdf-source-choice` 相关样式)
- Modify: `course.js:541-563` (showSubstate 添加 sourceChoice 状态)
- Modify: `course.js:614-620` (handlePdfFile 书签找到后改为显示来源选择)
- Modify: `course.js:640` (在 btnMineruExtract 监听后添加来源选择按钮监听)
- Modify: `strings.js` (添加 `bookmarkFound`、`useBookmark`、`useAiExtract`、`bookmarkCount` 键)
- Modify: `.agents/AGENT_GUIDELINES.md` (更新为 v=25)

### 背景

当前 `handlePdfFile()` 找到书签后直接调用 `onOutlineReady()`（展示"下一步"按钮），用户没有机会选择用 AI 提取。需要在书签找到后插入一个选择界面。

`showSubstate(state)` 函数（约 line 540）控制 step1 内各子状态的显示/隐藏，通过对比 state 字符串决定哪个 div 可见。

- [ ] **Step 1: 在 `course.html` 的 `pdf-no-outline` div 之前（约 line 292）插入来源选择 div**

在 `<div class="pdf-no-outline" id="pdfNoOutline" style="display:none">` 前插入：

```html
          <div class="pdf-source-choice" id="pdfSourceChoice" style="display:none">
            <p class="pdf-source-title" data-i18n="bookmarkFound">已找到 PDF 书签目录</p>
            <p class="pdf-source-count" id="pdfBookmarkCount"></p>
            <div class="source-choice-btns">
              <button class="btn-source-primary" id="btnUseBookmark" data-i18n="useBookmark">📑 使用 PDF 书签</button>
              <span class="source-or" data-i18n="orText">或</span>
              <button class="btn-source-ai" id="btnUseAI" data-i18n="useAiExtract">🤖 改用 AI 提取</button>
            </div>
          </div>
```

- [ ] **Step 2: 在 `course.css` 末尾添加来源选择样式**

```css
/* ── TOC Source Choice ──────────────────────────────────── */
.pdf-source-choice {
  flex-direction: column;
  align-items: center;
  gap: 14px;
  padding: 24px 0;
  text-align: center;
}
.pdf-source-title {
  font-size: 15px;
  font-weight: 600;
  color: var(--fg);
  margin: 0;
}
.pdf-source-count {
  font-size: 13px;
  color: var(--muted);
  margin: 0;
}
.source-choice-btns {
  display: flex;
  align-items: center;
  gap: 12px;
  flex-wrap: wrap;
  justify-content: center;
}
.source-or {
  color: var(--muted);
  font-size: 13px;
}
.btn-source-primary,
.btn-source-ai {
  padding: 9px 18px;
  border: 1px solid var(--border);
  border-radius: 8px;
  cursor: pointer;
  font-size: 13px;
  font-weight: 500;
  background: var(--card);
  color: var(--fg);
  transition: background .15s, border-color .15s;
}
.btn-source-primary:hover { background: var(--accent); border-color: var(--accent); color: #fff; }
.btn-source-ai:hover { background: rgba(139,92,246,.15); border-color: #8B5CF6; color: #a78bfa; }
```

- [ ] **Step 3: 修改 `course.js` 的 `showSubstate` 函数（约 line 541）**

找到 `showSubstate` 函数体，在所有 `display:none`/`display:flex` 切换行中添加 `pdfSourceChoice` 的处理。

找到类似这样的代码块（约 line 554）：
```js
  pdfDrop.style.display          = state === 'drop'      ? 'flex' : 'none';
```

在其后（但在函数结束前）添加一行：
```js
  document.getElementById('pdfSourceChoice').style.display = state === 'sourceChoice' ? 'flex' : 'none';
```

- [ ] **Step 4: 修改 `course.js` 的 `handlePdfFile` 函数（约 line 614）**

找到：
```js
    extractedOutline = normalizeOutline(outline);
    onOutlineReady();
```

替换为：
```js
    extractedOutline = normalizeOutline(outline);
    // Show source selection panel instead of going directly to step 2
    const chapterCount = extractedOutline.length;
    document.getElementById('pdfBookmarkCount').textContent =
      window.tf('bookmarkCount', { n: chapterCount });
    showSubstate('sourceChoice');
```

- [ ] **Step 5: 在 `course.js` 中 `btnMineruExtract` 的事件监听后（约 line 641）添加来源选择按钮监听**

```js
document.getElementById('btnUseBookmark').addEventListener('click', () => {
  showSubstate('drop');
  onOutlineReady();
});

document.getElementById('btnUseAI').addEventListener('click', () => {
  startMineruFlow();
});
```

- [ ] **Step 6: 在 `strings.js` 三个语言块各添加新键**

zh 块（在 `pdfBookmarkNote` 附近）：
```js
    bookmarkFound: '已找到 PDF 书签目录',
    useBookmark: '📑 使用 PDF 书签（推荐）',
    useAiExtract: '🤖 改用 AI 提取',
    bookmarkCount: '共 {n} 个顶级章节',
```

en 块对应位置：
```js
    bookmarkFound: 'PDF bookmarks found',
    useBookmark: '📑 Use PDF Bookmarks (recommended)',
    useAiExtract: '🤖 Use AI Extraction instead',
    bookmarkCount: '{n} top-level chapters',
```

es 块对应位置：
```js
    bookmarkFound: 'Marcadores PDF encontrados',
    useBookmark: '📑 Usar marcadores PDF (recomendado)',
    useAiExtract: '🤖 Usar extracción con IA',
    bookmarkCount: '{n} capítulos de nivel superior',
```

- [ ] **Step 7: 将 `course.html` 版本号从 `v=24` 改为 `v=25`，更新 AGENT_GUIDELINES 为 `v=25`**

- [ ] **Step 8: 手动验证**

  两个场景：
  1. 上传有书签的 PDF → 应出现来源选择面板，点"使用书签"→ 进入章节范围选择（step 2），点"改用 AI 提取"→ 进入 MinerU 流程
  2. 上传无书签的 PDF → 行为与修改前相同，直接进入 noOutline 状态

- [ ] **Step 9: Commit**

```bash
git add course.html course.js course.css strings.js .agents/AGENT_GUIDELINES.md
git commit -m "feat: add TOC source selection panel when PDF bookmarks exist (v=25)"
```

---

## Task 4 — P8a：资料删除二步确认

**Files:**
- Modify: `course.js:1479-1511` (renderMaterials 卡片 delete 按钮逻辑)
- Modify: `course.css` (添加 `.btn-mat-del-confirm` 样式)
- Modify: `strings.js` (添加 `confirmDeleteFile` 键)
- Modify: `course.html:496` (version v25 → v26)
- Modify: `.agents/AGENT_GUIDELINES.md` (更新为 v=26)

### 背景

`renderMaterials()` 中每个 card 的 `.btn-mat-del` 单击即删除，容易误触。需改为：第一次点击将按钮变为"⚠️ 确认删除？"，3 秒内第二次点击才真正删除，3 秒后自动复原。

使用闭包变量（per-card），不需要全局状态。

- [ ] **Step 1: 修改 `course.js` 的 `renderMaterials` 函数中的 delete 按钮事件监听（约 line 1505）**

找到：
```js
    card.querySelector('.btn-mat-del').addEventListener('click', async e => {
      e.stopPropagation();
      await dbDelete(f.id);
      await renderMaterials();
      showToast(window.t('deleted'));
    });
```

替换为：
```js
    const delBtn = card.querySelector('.btn-mat-del');
    let deleteArmed = false;
    let disarmTimer = null;
    delBtn.addEventListener('click', async e => {
      e.stopPropagation();
      if (!deleteArmed) {
        deleteArmed = true;
        delBtn.textContent = window.t('confirmDeleteFile');
        delBtn.classList.add('btn-mat-del-confirm');
        disarmTimer = setTimeout(() => {
          deleteArmed = false;
          delBtn.textContent = window.t('deleteFile');
          delBtn.classList.remove('btn-mat-del-confirm');
        }, 3000);
        return;
      }
      clearTimeout(disarmTimer);
      await dbDelete(f.id);
      await renderMaterials();
      showToast(window.t('deleted'));
    });
```

- [ ] **Step 2: 在 `course.css` 末尾添加确认状态样式**

```css
/* ── Two-step delete confirm state ──────────────────────── */
.btn-mat-del-confirm {
  background: rgba(239, 68, 68, .15) !important;
  border-color: #ef4444 !important;
  color: #f87171 !important;
}
```

- [ ] **Step 3: 在 `strings.js` 三个语言块添加 `confirmDeleteFile` 键**

zh 块（在 `deleteFile` 旁边）：
```js
    confirmDeleteFile: '⚠️ 确认删除？',
```

en 块：
```js
    confirmDeleteFile: '⚠️ Confirm delete?',
```

es 块：
```js
    confirmDeleteFile: '⚠️ ¿Confirmar eliminación?',
```

- [ ] **Step 4: 将版本号从 `v=25` 改为 `v=26`，更新 AGENT_GUIDELINES**

- [ ] **Step 5: 手动验证**

  打开 course 页面 → 课程资料区 → 点击某文件的"删除"按钮 → 确认按钮变红并显示"⚠️ 确认删除？" → 等 3 秒确认自动复原 → 再次点击"删除"后立即点"确认删除"→ 确认文件被删除

- [ ] **Step 6: Commit**

```bash
git add course.js course.css strings.js course.html .agents/AGENT_GUIDELINES.md
git commit -m "feat: require two-step confirmation before deleting course materials (v=26)"
```

---

## Task 5 — P8b：AI 可读文件选择

**Files:**
- Modify: `course.js:1479-1511` (renderMaterials 卡片添加文件选择按钮)
- Modify: `course.js:1783-1821` (loadMarkdownContext + refreshContextBadge 响应选择)
- Modify: `course.js` 顶部（添加 `selectedFiles` Set 及其持久化函数，约在 `renderMaterials` 函数前）
- Modify: `course.css` (添加 `.btn-mat-sel` 选中样式)
- Modify: `strings.js` (添加 `aiFileSelectTitle` 键)
- Modify: `course.html:496` (version v26 → v27)
- Modify: `.agents/AGENT_GUIDELINES.md` (更新为 v=27)

### 背景

`loadMarkdownContext()` 目前加载课程下所有 `.md` 文件。当资料很多时用户希望只让 AI 读特定文件，避免浪费 token 和处理时间。

选择状态存入 `localStorage` 键 `aiFileSelection_${courseId}`（JSON 数组，存文件 ID）。默认行为：如果用户从未选过文件（Set 为空），则读取所有 MD 文件（向后兼容）。

- [ ] **Step 1: 在 `course.js` 的 `renderMaterials` 函数前（约 line 1462）添加选择状态管理函数**

```js
// ── AI file selection ─────────────────────────────────────────────────────────
function loadFileSelection() {
  try {
    const ids = JSON.parse(localStorage.getItem('aiFileSelection_' + courseId) || '[]');
    return new Set(ids);
  } catch { return new Set(); }
}
function saveFileSelection(sel) {
  localStorage.setItem('aiFileSelection_' + courseId, JSON.stringify([...sel]));
}
let selectedFiles = loadFileSelection();
```

- [ ] **Step 2: 修改 `renderMaterials` 中的卡片生成逻辑（约 line 1479）**

找到：
```js
    const isText = f.type === 'text/plain' || (f.name || '').endsWith('.md') || (f.name || '').endsWith('.txt');
    const card = document.createElement('div');
    card.className = 'material-card';
    card.innerHTML = `
      <div class="material-card-icon">${fileIcon(f.type, f.name)}</div>
      <div class="material-card-name" title="${escHtml(f.name)}">${escHtml(f.name)}</div>
      <div class="material-card-meta">${fmtSize(f.size)} · ${new Date(f.addedAt).toLocaleDateString(appLocale())}</div>
      <div class="material-card-actions">
        <button class="btn-mat-open">${window.t('viewFile')}</button>
        <button class="btn-mat-rename">✎</button>
        <button class="btn-mat-del">${window.t('deleteFile')}</button>
      </div>`;
```

替换为：
```js
    const isText = f.type === 'text/plain' || (f.name || '').endsWith('.md') || (f.name || '').endsWith('.txt');
    const isMd   = (f.name || '').toLowerCase().endsWith('.md');
    const isSelectable = isMd;
    const isSelected   = selectedFiles.has(f.id);
    const card = document.createElement('div');
    card.className = 'material-card';
    card.innerHTML = `
      <div class="material-card-icon">${fileIcon(f.type, f.name)}</div>
      <div class="material-card-name" title="${escHtml(f.name)}">${escHtml(f.name)}</div>
      <div class="material-card-meta">${fmtSize(f.size)} · ${new Date(f.addedAt).toLocaleDateString(appLocale())}</div>
      <div class="material-card-actions">
        ${isSelectable ? `<button class="btn-mat-sel${isSelected ? ' selected' : ''}" title="${window.t('aiFileSelectTitle')}">🤖</button>` : ''}
        <button class="btn-mat-open">${window.t('viewFile')}</button>
        <button class="btn-mat-rename">✎</button>
        <button class="btn-mat-del">${window.t('deleteFile')}</button>
      </div>`;
```

然后在 `card.querySelector('.btn-mat-open')` 事件绑定前（约 line 1492）添加选择按钮事件：

```js
    if (isSelectable) {
      const selBtn = card.querySelector('.btn-mat-sel');
      selBtn.addEventListener('click', e => {
        e.stopPropagation();
        if (selectedFiles.has(f.id)) {
          selectedFiles.delete(f.id);
          selBtn.classList.remove('selected');
        } else {
          selectedFiles.add(f.id);
          selBtn.classList.add('selected');
        }
        saveFileSelection(selectedFiles);
        refreshContextBadge();
      });
    }
```

- [ ] **Step 3: 修改 `loadMarkdownContext` 函数（约 line 1800）**

找到：
```js
    const mds = all.filter(f => f.courseId === courseId && (f.name || '').toLowerCase().endsWith('.md'));
```

替换为：
```js
    const mds = all.filter(f => {
      if (f.courseId !== courseId) return false;
      if (!(f.name || '').toLowerCase().endsWith('.md')) return false;
      if (selectedFiles.size > 0) return selectedFiles.has(f.id);
      return true;
    });
```

- [ ] **Step 4: 修改 `refreshContextBadge` 函数（约 line 1786）**

找到：
```js
    const mds = all.filter(f => f.courseId === courseId && (f.name || '').toLowerCase().endsWith('.md'));
    const badge = document.getElementById('aiContextBadge');
    const countEl = document.getElementById('aiContextCount');
    if (badge && countEl) {
      badge.style.display = mds.length > 0 ? 'flex' : 'none';
      countEl.textContent = window.tf('filesCount', { n: mds.length });
    }
```

替换为：
```js
    const mds = all.filter(f => f.courseId === courseId && (f.name || '').toLowerCase().endsWith('.md'));
    const active = mds.filter(f => selectedFiles.size === 0 || selectedFiles.has(f.id));
    const badge = document.getElementById('aiContextBadge');
    const countEl = document.getElementById('aiContextCount');
    if (badge && countEl) {
      badge.style.display = active.length > 0 ? 'flex' : 'none';
      countEl.textContent = selectedFiles.size > 0
        ? window.tf('filesCount', { n: active.length }) + '/' + mds.length
        : window.tf('filesCount', { n: active.length });
    }
```

- [ ] **Step 5: 在 `course.css` 末尾添加文件选择按钮样式**

```css
/* ── AI file selection toggle ────────────────────────────── */
.btn-mat-sel {
  background: transparent;
  border: 1px solid var(--border);
  border-radius: 5px;
  padding: 2px 6px;
  font-size: 12px;
  cursor: pointer;
  color: var(--muted);
  opacity: .6;
  transition: opacity .15s, border-color .15s, color .15s;
}
.btn-mat-sel:hover { opacity: 1; border-color: #8B5CF6; }
.btn-mat-sel.selected {
  opacity: 1;
  border-color: #8B5CF6;
  color: #a78bfa;
  background: rgba(139,92,246,.12);
}
```

- [ ] **Step 6: 在 `strings.js` 三个语言块添加 `aiFileSelectTitle` 键**

zh 块：
```js
    aiFileSelectTitle: '切换此文件是否可被 AI 读取',
```

en 块：
```js
    aiFileSelectTitle: 'Toggle whether AI can read this file',
```

es 块：
```js
    aiFileSelectTitle: 'Alternar si la IA puede leer este archivo',
```

- [ ] **Step 7: 将版本号从 `v=26` 改为 `v=27`，更新 AGENT_GUIDELINES**

- [ ] **Step 8: 手动验证**

  场景 1：课程有 3 个 .md 文件 → 每个卡片出现 🤖 按钮 → 非 .md 文件无此按钮
  场景 2：点击某文件的 🤖 → 按钮高亮为紫色 → 刷新页面后状态保持 → AI 上下文徽章显示 "1/3"
  场景 3：全不选（Set 为空）→ AI 读取所有 md 文件（默认行为）

- [ ] **Step 9: Commit**

```bash
git add course.js course.css strings.js course.html .agents/AGENT_GUIDELINES.md
git commit -m "feat: allow user to select which files AI assistant can read (v=27)"
```

---

## 完成标志

所有 task 完成后：
- `course.js?v=27`（每个 task 各递增一次）
- 5 个 git commit（Task 1–5 各一个）
- `STATUS.md` TASK 板块标记完成
- ROADMAP P5–P8 全部标记 `[x]`
